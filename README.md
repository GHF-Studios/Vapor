# Vapor

Vapor is the root ecosystem layer for generic Engines, Games, Mods, packs, capability/Rhai authoring, validation,
launching protocol, and distribution.

The SDK and launcher live in separate repositories:

- `Vapor-SDK`
- `Vapor-Launcher`

Current contents:

- `vapor_core`
- `vapor_macros`
