# Root manifest for the Vapor ecosystem itself.
# Keep this file as intent: resolved Rust artifact hashes, exact URLs, and
# install receipts belong in generated lock/state files.

[project]
kind = "core"
id = "ghf-studios.vapor"

[toolchain]
channel = "nightly"
date = "2026-01-30"
