# Future Repos

Status: parked.

## Exists Now

- `Spacetime-Engine` and `Loo-Cast-Game` are future or related repos in the current roadmap.
- The old Loo-Cast repo is legacy/context, not the active implementation home.

## Needed Next

- Do not migrate Spacetime/Loo Cast specific pages unless active Vapor work needs the context.
- If such pages are temporarily hosted in Vapor, mark them as `legacy signal`.

## Total Target

- Spacetime Engine gets its own repo and docs when engine-specific work becomes active.
- Loo Cast gets its own repo and docs when game-specific work becomes active.
- Vapor stays generic.
