Decision Ledger 020c: Vapor / Loo-Cast Referee Queue

Status:
This is the current answerable form of decision ledger 020 after the second pivot.
The letter `c` is a queue-shape revision, not a new answered batch.
The old flat source ledger is preserved at:
`ai_conversation_logs/decision_ledger_020/history/020_flat_source_before_referee_rewrite.txt`

How this file should be used:

- The owner may answer any coherent prefix or subsection.
- Answers must be pasted back verbatim under the matching `A:` line.
- If an early answer changes later context, the assistant acts as referee:
  - preserve the answer exactly,
  - mark downstream items answered, superseded, invalidated, or needing rewrite,
  - rewrite the remaining unanswered queue into `020d`, `020e`, etc.,
  - keep everything under decision ledger index 020 unless the owner explicitly starts a new batch.
- The assistant must not treat defaults as doctrine.
- The assistant must not silently implement policy, naming, docs, or roadmap decisions from this queue.

Referee stop phrase:
If the owner writes something like "pivot check", "surely this pivots later questions", or equivalent, stop normal Q&A consumption and evaluate dependencies before continuing.

========================================================================
0. Referee Protocol
========================================================================

Q020a-001. Is this folder-and-letter model acceptable: `decision_ledger_020/020a_referee_queue.txt`, then `020b`, `020c`, etc. as rewritten working queues under the same ledger index?
A: Yes.

Q020a-002. Should the flat source file remain in `history/` as archaeology, while the latest lettered queue is the only thing you actively answer?
A: Yes.

Q020a-003. When you answer a section, should I paste your answer verbatim under `A:` even if it contains profanity, uncertainty, side comments, or later self-corrections?
A: Yes.

Q020a-004. Should I add short `REFEREE:` notes after your answers only when needed to explain downstream effects?
A: Yes.

Q020a-005. Should unanswered downstream questions be rewritten aggressively after a pivot, rather than preserved for historical purity?
A: Yes.

Q020a-006. Should defaults in this file be treated only as assistant working assumptions, never as accepted owner decisions?
A: Difficult to answer. Yes, but that itself is not to be taken as a set-in-stone rule, yk?

PIVOT CHECK 0:
If any answer above changes the file/revision protocol, stop here. The rest of the queue depends on this workflow.

========================================================================
1. Documentation Authority And Work Surface
========================================================================

Q020a-007. Should active documentation authority remain: root README first, crate rustdoc second, useful module rustdoc third, clear local names always preferred, and no `/docs` by default?
A: Yes, but the old glossary should indeed be moved/copied over into Vapor and promoted to canon material, yk?

Q020a-008. Should design intent, doctrine, roadmap, naming policy, workflow policy, and public command semantics require discussion before being written anywhere, including rustdoc and comments?
A: Yes.

Q020a-009. Should cold factual docs about what code currently does be allowed during approved implementation, as long as they do not add intent or doctrine?
A: Yes.

Q020a-010. Should legacy glossary pages be treated as high-signal quarry, but not as active doctrine and not as a surface we keep mutating during normal work?
A: Well, yes, but we *do* want it to be canon, not legacy. But I already said that; I think it's time for a pivot.

REFEREE:
The remaining queue is rewritten under this premise:
the old glossary is no longer merely non-canonical quarry. Its current physical location is legacy, but its substance should be migrated/promoted into Vapor as canon material, subject to owner-reviewed status and rewrite rules.

Q020b-011. Should `Loo-Cast/loo_cast_alpha/docs/glossary` be treated as the source corpus for a canonical Vapor concept corpus?
A: Yes.

Q020b-012. Should the old `Loo-Cast` repo still be treated as a legacy/context workspace even if its glossary content becomes canonical source material for Vapor?
A: Yes.

Q020b-013. Should "legacy glossary" now mean "legacy physical location/history", not "non-canonical concept content"?
A: Yes.

Q020b-014. Should Vapor make an explicit exception to the "no `/docs` by default" rule for a migrated canonical glossary/concept corpus?
A: Yes.

Q020b-015. Should the migrated corpus live at `Vapor/docs/glossary/`, or should it use a different path such as `Vapor/glossary/`, `Vapor/concepts/`, or `Vapor/canon/`?
A: Yes, `Vapor/docs/glossary/` is good.

Q020b-016. Should the folder name keep the word `glossary`, or should the content be renamed to avoid old process baggage?
A: Well, I mean, tbf we already saw the advantage of the glossary vs. tech glossary approach, so maybe we wanna capitalize on that and embrace it even more? Maybe a pivot soon? Lemme try answer some more before we pivot.

Q020b-017. Should the first migration be a mechanical copy of the whole glossary corpus, with no conceptual rewriting in the same change?
A: I suppose so, that also cleanly solves Q020b-016: Just split it into docs/glossary and docs/tech_glossary I suppose, and try and prevent cross-links between them.

Q020b-018. Should the first migration be selective instead, copying only Vapor-relevant pages and leaving USF/Spacetime/Loo-Cast-specific pages for future repos?
A: Actually, yes! This is a good fucking idea, yeeeeeeeeeeeeeeees!

Q020b-019. Should `glossary-summary.md`, `tech-glossary-summary.md`, and `glossary-alignment-issue-ledger.md` migrate with the glossary corpus?
A: Yes, but they also need to be subject to integration of new decisions and answers and such, they are not magical tokens of god-given absolute truth.

Q020b-020. Should old AI conversation logs from `Loo-Cast/loo_cast_alpha/docs/ai_conversation_logs` stay referenced history, or be copied into Vapor too?
A: Stay history.

Q020b-021. Should old diagrams, RFCs, and root docs stay out of Vapor unless separately approved?
A: Yes.

Q020b-022. Should migrated pages preserve Obsidian wikilinks/frontmatter/tags initially?
A: Yes!

Q020b-023. Should `.obsidian/` editor state remain excluded?
A: Nah.

Q020b-024. Should migrated pages keep current stale/WIP statuses at first, or should migration introduce a new status vocabulary immediately?
A: stale/WIP first. We wanna carefully introduce each concept one at a time and be very selective about it.

Q020b-025. Should canonical glossary pages be allowed to describe future/desired concepts not implemented yet?
A: Yeeeees, use them dangling obsidian/markdown links, yes!

Q020b-026. If yes, should every future/desired page carry explicit status such as `implemented`, `WIP`, `open pressure`, `deferred`, `legacy signal`, or `obsolete`?
A: Well, no. Only a much more minimal set of statuses possible, yk? Maybe just `implemented`, `WIP`, and just non-existent aka dangling link? Also `legacy signal` as a temporary transitionary state that cannot be re-entered by existing documents, only stuff newly transitioned over from the old glossary work but as-of-yet unapproved in it's entirety, yk?

Q020b-027. Should root README and rustdoc remain highest authority for implemented code behavior, while the migrated glossary becomes highest authority for conceptual/product vocabulary?
A: Yes.

Q020b-028. Should crate/module rustdoc link to canonical glossary pages only when the concept is implemented locally?
A: Yes.

Q020b-029. Should README avoid restating glossary doctrine and instead point to canonical glossary pages where needed?
A: Yes.

Q020b-030. Should AGENT files eventually be updated to include both the glossary-canon exception and the newer "design-intent docs require discussion" rule?
A: Yes.

Q020b-031. Should CLI help text, `CommandSpec` summaries, preconditions, and future-effects text be treated as documentation/policy surfaces requiring approval before semantic edits?
A: Yes, absolutely!

Q020b-032. Should the first post-migration review focus only on pages that affect current code work: Vapor Ecosystem, SDK, Vapor Launcher, Vapor Toolchain Envelope, Vapor.toml, Vapor.lock, Steam, Packagepack, Enginepack, Gamepack, Modpack, Fingerprint?
A: Well, yes, but I expect this list to grow significantly the more we specify pin down structure/architecture/infrastructure, yk?

REFEREE:
The old glossary should become a canonical Vapor concept corpus, but the first migration is no longer a simple whole-folder copy.
The queue now assumes:
- `Vapor/docs/glossary/` is accepted.
- A sibling `Vapor/docs/tech_glossary/` is likely desired.
- Migration should be selective and careful, despite preserving page formatting and Obsidian-style links.
- Summaries/ledger can migrate, but must be treated as mutable integration surfaces, not absolute truth.
- Old AI logs stay history.
- Old diagrams/RFC/root docs stay out unless separately approved.
- `.obsidian/` should not be excluded by default, but the exact handling still needs a concrete migration decision because it is editor state.
- Status vocabulary should stay minimal: probably `implemented`, `WIP`, dangling link absence, and temporary `legacy signal`.

Q020c-033. Should the immediate glossary migration be selective rather than whole-corpus, despite preserving the source corpus in history?
A:

Q020c-034. Should selected migrated pages be split into two destination folders: `Vapor/docs/glossary/` and `Vapor/docs/tech_glossary/`?
A:

Q020c-035. Should we actively prevent cross-links between `docs/glossary` and `docs/tech_glossary`, or merely avoid unnecessary cross-links?
A:

Q020c-036. Should `.obsidian/` be copied into Vapor as actual vault/editor configuration, or should only specific non-editor metadata be preserved?
A:

Q020c-037. If `.obsidian/` is copied, should it live under `Vapor/docs/.obsidian/`, repo root `.obsidian/`, or another path?
A:

Q020c-038. Should the first migration include only the current-code-impact starter list plus any pages required by direct links from those pages?
A:

Q020c-039. Should linked but not-yet-migrated pages remain dangling links until intentionally introduced?
A:

Q020c-040. Should migrated pages from the old corpus default to `legacy signal` until individually approved as `WIP` or `implemented`?
A:

Q020c-041. Should `implemented` be reserved only for concepts directly represented in active code or repo-local config?
A:

Q020c-042. Should `WIP` mean canonical but not fully implemented/finalized?
A:

Q020c-043. Should `legacy signal` be a one-way transition status only for old imported pages, as you described?
A:

Q020c-044. Should the summary docs and alignment ledger be migrated into `Vapor/docs/` root, or into a status/review subfolder under `Vapor/docs/`?
A:

Q020c-045. Should the first migration include a very small `Vapor/docs/README.md` explaining the glossary/tech_glossary split and status vocabulary, or should even that wait for a drafted approval packet?
A:

PIVOT CHECK 1:
If migration scope/path/status rules change here, repo topology and implementation priorities may need another rewrite before continuing.

========================================================================
2. Active Repo Topology
========================================================================

Q020c-046. Is the current active repo set exactly `Vapor`, `Vapor-SDK`, `Vapor-Launcher`, `Vapor-Examples`, and `Vapor-Registry`?
A:

Q020c-047. Should `Spacetime-Engine` and `Loo-Cast-Game` be treated as future/related repos for now, not active repos in the current implementation loop?
A:

Q020c-048. Should Vapor temporarily host selected Spacetime/Loo-Cast-related concept pages from the migrated glossary until those future repos exist?
A:

Q020c-049. If Vapor hosts those pages temporarily, should they be marked `legacy signal` or `WIP`, not `implemented`?
A:

Q020c-050. Should those temporary pages make clear that their eventual home may be a Spacetime or Loo Cast repo?
A:

Q020c-051. Should `Vapor` own foundational ecosystem concepts and crates such as `vapor_core`, `vapor_macros`, and maybe tiny shared protocol pieces?
A:

Q020c-052. Should `Vapor-SDK` own `vapor_sdk_core`, `vapor_sdk_cli`, and future `vapor_sdk_gui`?
A:

Q020c-053. Should `Vapor-Launcher` own `vapor_launcher_core`, `vapor_launcher_cli`, and future `vapor_launcher_gui`?
A:

Q020c-054. Should `Vapor-Examples` remain official example/test content and not depend on SDK or Launcher as ordinary Rust dependencies?
A:

Q020c-055. Should `Vapor-Registry` be treated as active context now, even if SDK does not enforce it yet?
A:

Q020c-056. Should `Vapor/AGENT.md`'s stack line `Vapor <- Spacetime-Engine <- Loo-Cast-Game` be interpreted as runtime/product stack only, while SDK/Launcher/Registry are operational sibling repos?
A:

PIVOT CHECK 2:
If the repo set or dependency direction changes, dependency policy and root-package questions need rewrite.

========================================================================
3. Cross-Repo Dependency Policy
========================================================================

Q020c-057. During development, should SDK/Launcher depend on Vapor through Git/branch dependencies by default, rather than sibling `path` dependencies?
A:

Q020c-058. Are the current SDK/Launcher `path = "../../../Vapor/crates/vapor_core"` dependencies acceptable as temporary bootstrap only?
A:

Q020c-059. Should release dependencies default to crates.io versions and proper Steam/distributed artifacts, not path or branch dependencies?
A:

Q020c-060. Should branch dependencies be allowed only in development/debug builds?
A:

Q020c-061. Should `vapor_core` be published to crates.io only once it has at least one real public responsibility, not as an empty reservation shell?
A:

Q020c-062. What is the minimum public responsibility that makes `vapor_core` publishable?
A:

Q020c-063. Should `vapor_steam` remain folded into `vapor_core` conceptually, with SDK owning only current SteamCMD implementation details?
A:

Q020c-064. Should `vapor_macros` remain present but unexpanded until a real macro-backed metadata surface exists?
A:

PIVOT CHECK 3:
If dependency policy changes here, Cargo.toml cleanup and release workflow questions need rewrite.

========================================================================
4. Product Identity, Steam Root, And App Boundary
========================================================================

Q020c-065. Is Steam AppID `2122620` currently best described as the first-party Vapor/Loo-Cast root app, rather than only "the game" or only "the SDK"?
A:

Q020c-066. Is DepotID `2122621` currently the root depot for first-party app-root files?
A:

Q020c-067. Should AppID/DepotID defaults stay as SDK constants with CLI overrides for now?
A:

Q020c-068. Should these IDs eventually come from `Vapor-Registry`, app-root config, root `Vapor.toml`, canonical glossary/config material, or some combination?
A:

Q020c-069. Should the Steam app root be the preferred development substrate for root work, even if local source checkouts still exist beside it?
A:

Q020c-070. Is hot-deploying SDK/Launcher binaries into the Steam-installed app root an accepted developer-only workflow, even though Steam may consider the local install modified?
A:

Q020c-071. Should normal player installs avoid hot-deploy/self-corruption patterns entirely?
A:

Q020c-072. Is `sdk_cli` the intended long-term shipped command name, or only the current app-root alias?
A:

Q020c-073. Is `launcher_cli` the intended long-term shipped command name, or only the current app-root alias?
A:

PIVOT CHECK 4:
If the root app identity changes here, root packaging, SteamCMD, registry, and self-bootstrap questions need rewrite.

========================================================================
5. Steam Root Packaging And Self-Bootstrap
========================================================================

Q020c-074. Should the current root package continue to include only app-root CLI surfaces and activation scripts, excluding `rustup-home`, `cargo-home`, and `output`?
A:

Q020c-075. Should the Steam root package eventually include a full app-local Rust toolchain, or should the downloaded SDK acquire/reconcile the toolchain after install?
A:

Q020c-076. Should canonical glossary/docs material be included in the Steam root package?
A:

Q020c-077. If included, should canonical docs be player-visible, SDK/dev-visible, or only present as source/context files?
A:

Q020c-078. Should root packaging fail if SDK CLI inputs are missing?
A:

Q020c-079. Should root packaging fail if Launcher CLI inputs are missing while launcher is still mostly stubbed?
A:

Q020c-080. Should the current Launcher placeholder output be replaced before any user-facing Steam build includes it?
A:

Q020c-081. What minimum proof makes the self-bootstrap loop real: Steam downloads root, downloaded SDK builds/checks/deploys/packages/publishes the next root?
A:

Q020c-082. Should generated SteamPipe VDFs under `output/root/scripts` be considered build/release artifacts, not source files?
A:

Q020c-083. Should default Steam branch activation remain manual, with `--set-live` only for beta branches?
A:

Q020c-084. Should the Steam account name remain operator-provided CLI input and never be hardcoded/configured in repo files?
A:

PIVOT CHECK 5:
If the root package should include toolchains or canonical docs directly, Rustup acquisition and packaging questions need rewrite.

========================================================================
6. SteamCMD, Registry, And Release Authority
========================================================================

Q020c-085. For now, is Steamworks/SteamCMD permission enough to authorize root publish?
A:

Q020c-086. Should SDK root publish consult `Vapor-Registry` permissions before invoking SteamCMD?
A:

Q020c-087. If registry checks are added, should they be advisory diagnostics first or hard blockers immediately?
A:

Q020c-088. Should GitHub identity and Steam identity be bound now, later, or not at all?
A:

Q020c-089. Should SDK manage/acquire SteamCMD app-locally, or may it rely on `steamcmd` from PATH for the near future?
A:

Q020c-090. Should SteamCMD state/login tokens be forced under `$VAPOR_HOME/steam/steamcmd`, or is system SteamCMD state acceptable for now?
A:

Q020c-091. Should SteamCMD-only root publishing remain the first Steam slice before Steamworks API, Workshop UGC, or entitlement checks?
A:

Q020c-092. Should AppID ownership checks gate all SDK usage, only public/published workflows, or only later Workshop workflows?
A:

PIVOT CHECK 6:
If release authority moves to the registry now, root publish implementation and identity questions need rewrite.

========================================================================
7. Rust Toolchain Portability
========================================================================

Q020c-093. Should the Rustup-vs-standalone-archives decision be made only after a relocation test of the current app-root layout?
A:

Q020c-094. What exact relocation test should decide whether current Rustup wrapping is acceptable?
A:

Q020c-095. Is system PATH Rustup acceptable for development bootstrap only?
A:

Q020c-096. Should Steam-distributed SDK installs prefer an app-local `$VAPOR_HOME/rustup/bin/rustup` if Rustup remains the backend?
A:

Q020c-097. Should the next toolchain slice implement app-local Rustup acquisition before considering standalone Rust archives?
A:

Q020c-098. If relocation fails because Rustup metadata dislikes the move, should repair first try to reconcile/reinstall Rustup around the existing toolchain before redownloading everything?
A:

Q020c-099. Should `toolchain install` become idempotent success when the expected toolchain is already present and minimally verified?
A:

Q020c-100. What minimum verification should replace today's `PresentUnverified` file-existence check?
A:

Q020c-101. Should "SDK toolchain install" and "Vapor Toolchain Envelope for native kernel ABI compatibility" be treated as separate concepts?
A:

Q020c-102. What failure evidence would justify returning to standalone official Rust archives?
A:

PIVOT CHECK 7:
If Rustup is rejected or toolchain packaging changes, target and workspace command questions need rewrite.

========================================================================
8. Targets And Cross-Compilation
========================================================================

Q020c-103. Should `x86_64-pc-windows-gnu` become the first Windows cross-build proof target from Arch/Linux?
A:

Q020c-104. Should `x86_64-pc-windows-msvc` remain a later important target because of Steamworks, GUI/native dependencies, or ecosystem expectations?
A:

Q020c-105. Should host triples, installable stdlib targets, linkable build targets, and release-supported targets be modeled separately?
A:

Q020c-106. Should target stdlibs be installed eagerly with the canonical toolchain, or on demand per target command?
A:

Q020c-107. Should linker/tool requirements be explicit SDK status/check output before claiming target support?
A:

Q020c-108. Should Windows GNU proof be allowed even if it cannot be final release policy?
A:

PIVOT CHECK 8:
If Windows target policy changes, toolchain constants and install-plan questions need rewrite.

========================================================================
9. Workspace Execution And Hermeticity
========================================================================

Q020c-109. Should Vapor-managed `cargo check/fmt/build` preserve system PATH after Vapor path prefixes for now?
A:

Q020c-110. Is preserving system PATH incompatible with saying "no system tool leaks"?
A:

Q020c-111. Which environment variables must be scrubbed before claiming hermetic Cargo/Rust execution?
A:

Q020c-112. Should build outputs stay under `$VAPOR_HOME/output/dev/<workspace>` for Vapor-managed commands?
A:

Q020c-113. Should source repo `target/` directories remain unused by Vapor-managed commands?
A:

Q020c-114. Should `workspace sync` prompt because it writes files?
A:

Q020c-115. Should `sdk fmt` prompt because it writes files, or is formatting a normal authoring command?
A:

Q020c-116. Should `workspace deploy` remain an SDK command, or is it really internal root/app maintenance?
A:

Q020c-117. Should SDK replace `xtask` for first-party root work, or should xtask-like internal tooling remain separate?
A:

PIVOT CHECK 9:
If hermeticity expectations change, relocation tests and workspace cleanup need rewrite.

========================================================================
10. Manifest, Artifact, Lockfile, Fingerprint
========================================================================

Q020c-118. Should the current active `Vapor.toml` schema stay minimal for now: workspace identity, content graph, root toolchain pin, and registry metadata?
A:

Q020c-119. Should every generated content crate in `Vapor-Examples/crates/*` have its own `Vapor.toml`?
A:

Q020c-120. Are managed-file headers acceptable TOML comments because they prevent overwriting user-owned files?
A:

Q020c-121. Should SDK fully manage generated Cargo workspace files for custom-content workspaces, or should users own Cargo files and SDK only validate?
A:

Q020c-122. Should seeded `lib.rs` files remain user-owned after creation?
A:

Q020c-123. Should seeded files avoid crate-level intent rustdoc until content semantics are discussed?
A:

Q020c-124. What should be the first `Vapor.lock`-like artifact: packagepack lock, toolchain receipt, root publish receipt, install ledger, canonical glossary migration receipt, or something else?
A:

Q020c-125. Should the first fingerprint implementation be semantic plaintext/TOML first, hash first, or both?
A:

Q020c-126. Which artifact terms may active code use now without causing terminology drift?
A:

Q020c-127. Should active code reserve `root package` for Steam app/depot updates and `Packagepack` for launch composition?
A:

PIVOT CHECK 10:
If the manifest/lockfile first slice changes, pack validation and SDK command questions need rewrite.

========================================================================
11. Packs, Capability, Rhai, Runtime Concepts
========================================================================

Q020c-128. Should the migrated canonical glossary be allowed to answer some pack/capability/Rhai terminology before code implements it?
A:

Q020c-129. Should the next pack implementation validate content-kind child rules before capability/Rhai semantics?
A:

Q020c-130. Should current `engine` content represent a placeholder generic Engine until `core_engine`/`core_mod` artifact layout exists?
A:

Q020c-131. Should current `game` content represent a placeholder generic Game until `base_mod` artifact layout exists?
A:

Q020c-132. Should active code avoid enforcing reserved names like `core_engine`, `core_mod`, and `base_mod` until actual Engine/Game artifact layout exists?
A:

Q020c-133. Should Extension Mods have one attachment parent plus normal dependencies, or can they have multiple attachment parents?
A:

Q020c-134. Should first Rhai work be parser-free file classification before Rhai execution or linting?
A:

Q020c-135. Should SDK `validate` remain manifest/graph-only until Rhai semantics are cleaner?
A:

Q020c-136. What is the smallest capability concept worth implementing in `vapor_core`?
A:

Q020c-137. Should active code avoid names still flagged as suspect: `Vapor Platform`, `Capability Profile`, `Capability Declaration`, `Rhai Asset`, and `Runtime Lock`?
A:

Q020c-138. Should toolchain work avoid all kernel ABI / native plugin compatibility claims until native plugin loading is real?
A:

PIVOT CHECK 11:
If capability/Rhai enters earlier than expected, manifest, lockfile, and toolchain-envelope questions need rewrite.

========================================================================
12. Workshop, Trust, Install Ledger
========================================================================

Q020c-139. Should root app/toolchain/bootstrap and canonical glossary migration stay ahead of Workshop content release work?
A:

Q020c-140. Which repo should own the first Workshop implementation: SDK for upload/publish, Launcher for install/launch, shared core types later?
A:

Q020c-141. Where should authoritative fingerprints live before registry enforcement exists: uploaded content, local lock/receipt, registry, canonical glossary metadata, or all of these?
A:

Q020c-142. Should wording be "trusted but validated for compatibility/integrity" rather than "untrusted hostile content"?
A:

Q020c-143. Should signing fields stay out of active schemas for now?
A:

Q020c-144. Should install ledger belong to Launcher, SDK, shared app-root state, or separate per-workflow receipts?
A:

Q020c-145. Should local dev artifacts ever override Workshop artifacts automatically?
A:

Q020c-146. Should local dev artifact selection require explicit developer/modpack-author action?
A:

Q020c-147. Should Steam Web API/private backend work be avoided until a concrete SteamCMD/Steamworks gap requires it?
A:

PIVOT CHECK 12:
If Workshop moves earlier, root bootstrap, glossary migration, and registry priorities need rewrite.

========================================================================
13. Command Surface And Safety
========================================================================

Q020c-148. Should broad SDK command stubs remain visible for planning, or should commands be hidden until implemented?
A:

Q020c-149. Which SDK command families must become real before further broad command expansion?
A:

Q020c-150. Should current command names be treated as disposable pre-Phase-3 scaffolding?
A:

Q020c-151. Should root SteamPipe publish and future Workshop content publish stay sharply separated in command names?
A:

Q020c-152. Should SDK and Launcher keep shared global flags `--yes`, `--force`, `--strict`, and `--keep-unused-versions` for now?
A:

Q020c-153. Should `--force` semantics be delayed until each mutation is real?
A:

Q020c-154. Should Launcher placeholder output become factual `status: not_implemented` style output?
A:

Q020c-155. Should CLI output eventually support machine-readable JSON for GUI/tooling, or is that premature?
A:

PIVOT CHECK 13:
If command visibility changes, SDK/Launcher cleanup questions need rewrite.

========================================================================
14. Code Cleanup Permission Boundaries
========================================================================

Q020c-156. Is splitting `workspace/manage.rs` into smaller behavior-identical modules approved once an exact file list is presented?
A:

Q020c-157. Is splitting `workspace/deploy.rs` into smaller behavior-identical modules approved once an exact file list is presented?
A:

Q020c-158. Should `toolchain.rs` be split only after relocation testing, unless the extraction is purely mechanical?
A:

Q020c-159. Should SDK get a shared app-root layout module to deduplicate alias/executable/activation paths?
A:

Q020c-160. Is adding or changing module rustdoc allowed only for cold facts unless the exact intent wording has been approved?
A:

Q020c-161. Should SDK/Launcher duplicate CLI safety/spec scaffolding until semantics stabilize, rather than introducing a shared crate?
A:

Q020c-162. Should root package symlink behavior remain "copy target contents" for now?
A:

Q020c-163. Should root package reject directories unless explicit ignore rules exist?
A:

Q020c-164. Should `vapor_core` keep owning the canonical toolchain model, or should that move to SDK later?
A:

Q020c-165. What is the first cleanup slice you would approve after this ledger pass?
A:

PIVOT CHECK 14:
If cleanup permission is narrow, implementation plans must be rewritten as tiny approval packets.

========================================================================
15. Immediate Next Action
========================================================================

Q020c-166. After answering enough of this queue, should the next actual work be a canonical glossary migration plan?
A:

Q020c-167. Should that migration plan be presented as exact source paths, destination paths, selected pages, excluded files, `.obsidian` handling, status policy, and no-rewrite guarantees before running anything?
A:

Q020c-168. Should the next actual work instead be a relocation-test plan?
A:

Q020c-169. Should the relocation-test plan be presented as exact commands, expected filesystem mutations, and pass/fail criteria before running anything?
A:

Q020c-170. Should the next actual work instead be a dependency-policy patch plan for SDK/Launcher `vapor_core` dependencies?
A:

Q020c-171. Should the next actual work instead be a root-package/self-bootstrap plan?
A:

Q020c-172. Should the next actual work instead be a tiny code-cleanup packet with no policy changes?
A:

Q020c-173. When you stop answering and ask for a pivot check, should I rewrite the unanswered remainder into the next lettered queue before doing any implementation?
A:

END OF 020c
