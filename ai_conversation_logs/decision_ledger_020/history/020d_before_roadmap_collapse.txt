Decision Ledger 020d: Vapor / Loo-Cast Referee Queue

Status:
This is the current answerable form of decision ledger 020 after the third pivot.
The letter `d` is a queue-shape revision, not a new answered batch.
The old flat source ledger is preserved at:
`ai_conversation_logs/decision_ledger_020/history/020_flat_source_before_referee_rewrite.txt`

How this file should be used:

- The owner may answer any coherent prefix or subsection.
- Answers must be pasted back verbatim under the matching `A:` line.
- If an early answer changes later context, the assistant acts as referee:
  - preserve the answer exactly,
  - mark downstream items answered, superseded, invalidated, or needing rewrite,
  - rewrite the remaining unanswered queue into `020e`, `020f`, etc.,
  - keep everything under decision ledger index 020 unless the owner explicitly starts a new batch.
- The assistant must not treat defaults as doctrine.
- The assistant must not silently implement policy, naming, docs, or roadmap decisions from this queue.

Referee stop phrase:
If the owner writes something like "pivot check", "surely this pivots later questions", or equivalent, stop normal Q&A consumption and evaluate dependencies before continuing.

========================================================================
0. Referee Protocol
========================================================================

Q020a-001. Is this folder-and-letter model acceptable: `decision_ledger_020/020a_referee_queue.txt`, then `020b`, `020c`, etc. as rewritten working queues under the same ledger index?
A: Yes.

Q020a-002. Should the flat source file remain in `history/` as archaeology, while the latest lettered queue is the only thing you actively answer?
A: Yes.

Q020a-003. When you answer a section, should I paste your answer verbatim under `A:` even if it contains profanity, uncertainty, side comments, or later self-corrections?
A: Yes.

Q020a-004. Should I add short `REFEREE:` notes after your answers only when needed to explain downstream effects?
A: Yes.

Q020a-005. Should unanswered downstream questions be rewritten aggressively after a pivot, rather than preserved for historical purity?
A: Yes.

Q020a-006. Should defaults in this file be treated only as assistant working assumptions, never as accepted owner decisions?
A: Difficult to answer. Yes, but that itself is not to be taken as a set-in-stone rule, yk?

PIVOT CHECK 0:
If any answer above changes the file/revision protocol, stop here. The rest of the queue depends on this workflow.

========================================================================
1. Documentation Authority And Canon Glossary
========================================================================

Q020a-007. Should active documentation authority remain: root README first, crate rustdoc second, useful module rustdoc third, clear local names always preferred, and no `/docs` by default?
A: Yes, but the old glossary should indeed be moved/copied over into Vapor and promoted to canon material, yk?

Q020a-008. Should design intent, doctrine, roadmap, naming policy, workflow policy, and public command semantics require discussion before being written anywhere, including rustdoc and comments?
A: Yes.

Q020a-009. Should cold factual docs about what code currently does be allowed during approved implementation, as long as they do not add intent or doctrine?
A: Yes.

Q020a-010. Should legacy glossary pages be treated as high-signal quarry, but not as active doctrine and not as a surface we keep mutating during normal work?
A: Well, yes, but we *do* want it to be canon, not legacy. But I already said that; I think it's time for a pivot.

REFEREE:
The old glossary is no longer merely non-canonical quarry. Its current physical location is legacy, but its substance should be migrated/promoted into Vapor as canon material, subject to owner-reviewed status and rewrite rules.

Q020b-011. Should `Loo-Cast/loo_cast_alpha/docs/glossary` be treated as the source corpus for a canonical Vapor concept corpus?
A: Yes.

Q020b-012. Should the old `Loo-Cast` repo still be treated as a legacy/context workspace even if its glossary content becomes canonical source material for Vapor?
A: Yes.

Q020b-013. Should "legacy glossary" now mean "legacy physical location/history", not "non-canonical concept content"?
A: Yes.

Q020b-014. Should Vapor make an explicit exception to the "no `/docs` by default" rule for a migrated canonical glossary/concept corpus?
A: Yes.

Q020b-015. Should the migrated corpus live at `Vapor/docs/glossary/`, or should it use a different path such as `Vapor/glossary/`, `Vapor/concepts/`, or `Vapor/canon/`?
A: Yes, `Vapor/docs/glossary/` is good.

Q020b-016. Should the folder name keep the word `glossary`, or should the content be renamed to avoid old process baggage?
A: Well, I mean, tbf we already saw the advantage of the glossary vs. tech glossary approach, so maybe we wanna capitalize on that and embrace it even more? Maybe a pivot soon? Lemme try answer some more before we pivot.

Q020b-017. Should the first migration be a mechanical copy of the whole glossary corpus, with no conceptual rewriting in the same change?
A: I suppose so, that also cleanly solves Q020b-016: Just split it into docs/glossary and docs/tech_glossary I suppose, and try and prevent cross-links between them.

Q020b-018. Should the first migration be selective instead, copying only Vapor-relevant pages and leaving USF/Spacetime/Loo-Cast-specific pages for future repos?
A: Actually, yes! This is a good fucking idea, yeeeeeeeeeeeeeeees!

Q020b-019. Should `glossary-summary.md`, `tech-glossary-summary.md`, and `glossary-alignment-issue-ledger.md` migrate with the glossary corpus?
A: Yes, but they also need to be subject to integration of new decisions and answers and such, they are not magical tokens of god-given absolute truth.

Q020b-020. Should old AI conversation logs from `Loo-Cast/loo_cast_alpha/docs/ai_conversation_logs` stay referenced history, or be copied into Vapor too?
A: Stay history.

Q020b-021. Should old diagrams, RFCs, and root docs stay out of Vapor unless separately approved?
A: Yes.

Q020b-022. Should migrated pages preserve Obsidian wikilinks/frontmatter/tags initially?
A: Yes!

Q020b-023. Should `.obsidian/` editor state remain excluded?
A: Nah.

Q020b-024. Should migrated pages keep current stale/WIP statuses at first, or should migration introduce a new status vocabulary immediately?
A: stale/WIP first. We wanna carefully introduce each concept one at a time and be very selective about it.

Q020b-025. Should canonical glossary pages be allowed to describe future/desired concepts not implemented yet?
A: Yeeeees, use them dangling obsidian/markdown links, yes!

Q020b-026. If yes, should every future/desired page carry explicit status such as `implemented`, `WIP`, `open pressure`, `deferred`, `legacy signal`, or `obsolete`?
A: Well, no. Only a much more minimal set of statuses possible, yk? Maybe just `implemented`, `WIP`, and just non-existent aka dangling link? Also `legacy signal` as a temporary transitionary state that cannot be re-entered by existing documents, only stuff newly transitioned over from the old glossary work but as-of-yet unapproved in it's entirety, yk?

Q020b-027. Should root README and rustdoc remain highest authority for implemented code behavior, while the migrated glossary becomes highest authority for conceptual/product vocabulary?
A: Yes.

Q020b-028. Should crate/module rustdoc link to canonical glossary pages only when the concept is implemented locally?
A: Yes.

Q020b-029. Should README avoid restating glossary doctrine and instead point to canonical glossary pages where needed?
A: Yes.

Q020b-030. Should AGENT files eventually be updated to include both the glossary-canon exception and the newer "design-intent docs require discussion" rule?
A: Yes.

Q020b-031. Should CLI help text, `CommandSpec` summaries, preconditions, and future-effects text be treated as documentation/policy surfaces requiring approval before semantic edits?
A: Yes, absolutely!

Q020b-032. Should the first post-migration review focus only on pages that affect current code work: Vapor Ecosystem, SDK, Vapor Launcher, Vapor Toolchain Envelope, Vapor.toml, Vapor.lock, Steam, Packagepack, Enginepack, Gamepack, Modpack, Fingerprint?
A: Well, yes, but I expect this list to grow significantly the more we specify pin down structure/architecture/infrastructure, yk?

REFEREE:
The old glossary should become a canonical Vapor concept corpus, but the first migration is selective.

Q020c-033. Should the immediate glossary migration be selective rather than whole-corpus, despite preserving the source corpus in history?
A: Yes. Spacetime and Loo-Cast are not our focus rn.

Q020c-034. Should selected migrated pages be split into two destination folders: `Vapor/docs/glossary/` and `Vapor/docs/tech_glossary/`?
A: Yes, in fact the original material is already split this way, but ofc even that split must be scrutinized.

Q020c-035. Should we actively prevent cross-links between `docs/glossary` and `docs/tech_glossary`, or merely avoid unnecessary cross-links?
A: I mean, just keep em to a set of specific entrypoints, yk? No huge bridges, we want this to truly be like a technical reflection of the same concept but like from the other side of the coin, yk?

Q020c-036. Should `.obsidian/` be copied into Vapor as actual vault/editor configuration, or should only specific non-editor metadata be preserved?
A: Fuck that, nuke .obsidian, there is nothing significant in there I would care about.

Q020c-037. If `.obsidian/` is copied, should it live under `Vapor/docs/.obsidian/`, repo root `.obsidian/`, or another path?
A: N/A. Still no explicit pivot, cause this is (still) straightforward.

Q020c-038. Should the first migration include only the current-code-impact starter list plus any pages required by direct links from those pages?
A: Yes, but don't delude yourself into thinking that this will not be quite the enormous batch, cause it will be.

Q020c-039. Should linked but not-yet-migrated pages remain dangling links until intentionally introduced?
A: Yes.

Q020c-040. Should migrated pages from the old corpus default to `legacy signal` until individually approved as `WIP` or `implemented`?
A: Yes.

Q020c-041. Should `implemented` be reserved only for concepts directly represented in active code or repo-local config?
A: Yes.

Q020c-042. Should `WIP` mean canonical but not fully implemented/finalized?
A: Yes. Canonical, but not trustworthy.

Q020c-043. Should `legacy signal` be a one-way transition status only for old imported pages, as you described?
A: Yes.

Q020c-044. Should the summary docs and alignment ledger be migrated into `Vapor/docs/` root, or into a status/review subfolder under `Vapor/docs/`?
A: Hm, I think we wanna use the docs root, yes, but we want to be mindful of what and how much we put there.

Q020c-045. Should the first migration include a very small `Vapor/docs/README.md` explaining the glossary/tech_glossary split and status vocabulary, or should even that wait for a drafted approval packet?
A: Indeed, even that has to be approved first. Time for a pivot.

REFEREE:
The docs migration is now specific enough to drive an implementation packet, but not specific enough to run one without approval.
Assumptions for the remaining queue:
- Migration is selective, focused on Vapor/current-code-impact concepts.
- `Vapor/docs/glossary/` and `Vapor/docs/tech_glossary/` are accepted destinations, subject to scrutinizing the split.
- Cross-links between glossary and tech glossary should exist only through specific entrypoints.
- `.obsidian/` is excluded/nuked.
- Selected direct-link dependencies may make the first batch large.
- Non-migrated links may remain dangling intentionally.
- Imported pages default to `legacy signal`.
- `implemented` means represented in active code or repo-local config.
- `WIP` means canonical but not trustworthy/final.
- Summary docs and alignment ledger may live in `Vapor/docs/` root, but need restraint.
- Even a tiny `Vapor/docs/README.md` requires a drafted approval packet first.

Q020d-046. Should the immediate next implementation packet be a glossary migration plan, before repo topology/dependency/toolchain implementation work?
A: I- yes, but I wanted more of a roadmap with descriptions and sub-roadmaps and all, yk? Not jus "what to do next". I was hoping to get a fairly detailed and walled-off topology of roadmaps basically, if that makes any sense man. Just markdown files and folder nesting would do the job. So we do need our glossary, and we need a new docs section "roadmap", containing the root roadmap basically, yk?

Q020d-047. Should that packet include a proposed `Vapor/docs/README.md` draft for approval before any file creation?
A: Yes.

Q020d-048. Should the migration packet list every selected source page and destination path explicitly?
A: Yes.

Q020d-049. Should the migration packet list every intentionally dangling link created by the selected page set?
A: Yes.

Q020d-050. Should the first migration batch include the summaries and alignment ledger at `Vapor/docs/`, or should those wait until after the first page copy?
A: No, don't wait.

Q020d-051. Should the first migration batch include only pages directly related to current active code surfaces, even if that leaves core conceptual context dangling?
A: Yes.

Q020d-052. Should `legacy signal` be applied mechanically to every imported page in the first migration?
A: Yes.

Q020d-053. Should any imported page be upgraded to `WIP` or `implemented` in the first migration, or should that require a later review patch?
A: Only after I/we approve. Nothing more than that, nothing less than that.

Q020d-054. Should the migration packet include no rewrites besides path/status/frontmatter adjustments required by the migration?
A: Yes.

PIVOT CHECK 1:
If the immediate next implementation is not glossary migration, repo topology continues below.
If glossary migration is next, the rest of this queue remains useful but should probably pause until the migration packet is drafted.

========================================================================
2. Active Repo Topology
========================================================================

Q020d-055. Is the current active repo set exactly `Vapor`, `Vapor-SDK`, `Vapor-Launcher`, `Vapor-Examples`, and `Vapor-Registry`?
A:

Q020d-056. Should `Spacetime-Engine` and `Loo-Cast-Game` be treated as future/related repos for now, not active repos in the current implementation loop?
A:

Q020d-057. Should Vapor temporarily host selected Spacetime/Loo-Cast-related concept pages from the migrated glossary only when they are required context for active Vapor concepts?
A:

Q020d-058. If Vapor hosts those pages temporarily, should they be marked `legacy signal`, not `WIP` or `implemented`, until their owning repo exists or owner review promotes them?
A:

Q020d-059. Should those temporary pages make clear that their eventual home may be a Spacetime or Loo Cast repo?
A:

Q020d-060. Should `Vapor` own foundational ecosystem concepts and crates such as `vapor_core`, `vapor_macros`, and maybe tiny shared protocol pieces?
A:

Q020d-061. Should `Vapor-SDK` own `vapor_sdk_core`, `vapor_sdk_cli`, and future `vapor_sdk_gui`?
A:

Q020d-062. Should `Vapor-Launcher` own `vapor_launcher_core`, `vapor_launcher_cli`, and future `vapor_launcher_gui`?
A:

Q020d-063. Should `Vapor-Examples` remain official example/test content and not depend on SDK or Launcher as ordinary Rust dependencies?
A:

Q020d-064. Should `Vapor-Registry` be treated as active context now, even if SDK does not enforce it yet?
A:

Q020d-065. Should `Vapor/AGENT.md`'s stack line `Vapor <- Spacetime-Engine <- Loo-Cast-Game` be interpreted as runtime/product stack only, while SDK/Launcher/Registry are operational sibling repos?
A:

PIVOT CHECK 2:
If the repo set or dependency direction changes, dependency policy and root-package questions need rewrite.

========================================================================
3. Cross-Repo Dependency Policy
========================================================================

Q020d-066. During development, should SDK/Launcher depend on Vapor through Git/branch dependencies by default, rather than sibling `path` dependencies?
A:

Q020d-067. Are the current SDK/Launcher `path = "../../../Vapor/crates/vapor_core"` dependencies acceptable as temporary bootstrap only?
A:

Q020d-068. Should release dependencies default to crates.io versions and proper Steam/distributed artifacts, not path or branch dependencies?
A:

Q020d-069. Should branch dependencies be allowed only in development/debug builds?
A:

Q020d-070. Should `vapor_core` be published to crates.io only once it has at least one real public responsibility, not as an empty reservation shell?
A:

Q020d-071. What is the minimum public responsibility that makes `vapor_core` publishable?
A:

Q020d-072. Should `vapor_steam` remain folded into `vapor_core` conceptually, with SDK owning only current SteamCMD implementation details?
A:

Q020d-073. Should `vapor_macros` remain present but unexpanded until a real macro-backed metadata surface exists?
A:

PIVOT CHECK 3:
If dependency policy changes here, Cargo.toml cleanup and release workflow questions need rewrite.

========================================================================
4. Product Identity, Steam Root, And App Boundary
========================================================================

Q020d-074. Is Steam AppID `2122620` currently best described as the first-party Vapor/Loo-Cast root app, rather than only "the game" or only "the SDK"?
A:

Q020d-075. Is DepotID `2122621` currently the root depot for first-party app-root files?
A:

Q020d-076. Should AppID/DepotID defaults stay as SDK constants with CLI overrides for now?
A:

Q020d-077. Should these IDs eventually come from `Vapor-Registry`, app-root config, root `Vapor.toml`, canonical glossary/config material, or some combination?
A:

Q020d-078. Should the Steam app root be the preferred development substrate for root work, even if local source checkouts still exist beside it?
A:

Q020d-079. Is hot-deploying SDK/Launcher binaries into the Steam-installed app root an accepted developer-only workflow, even though Steam may consider the local install modified?
A:

Q020d-080. Should normal player installs avoid hot-deploy/self-corruption patterns entirely?
A:

Q020d-081. Is `sdk_cli` the intended long-term shipped command name, or only the current app-root alias?
A:

Q020d-082. Is `launcher_cli` the intended long-term shipped command name, or only the current app-root alias?
A:

PIVOT CHECK 4:
If the root app identity changes here, root packaging, SteamCMD, registry, and self-bootstrap questions need rewrite.

========================================================================
5. Steam Root Packaging And Self-Bootstrap
========================================================================

Q020d-083. Should the current root package continue to include only app-root CLI surfaces and activation scripts, excluding `rustup-home`, `cargo-home`, and `output`?
A:

Q020d-084. Should the Steam root package eventually include a full app-local Rust toolchain, or should the downloaded SDK acquire/reconcile the toolchain after install?
A:

Q020d-085. Should canonical glossary/docs material be included in the Steam root package?
A:

Q020d-086. If included, should canonical docs be player-visible, SDK/dev-visible, or only present as source/context files?
A:

Q020d-087. Should root packaging fail if SDK CLI inputs are missing?
A:

Q020d-088. Should root packaging fail if Launcher CLI inputs are missing while launcher is still mostly stubbed?
A:

Q020d-089. Should the current Launcher placeholder output be replaced before any user-facing Steam build includes it?
A:

Q020d-090. What minimum proof makes the self-bootstrap loop real: Steam downloads root, downloaded SDK builds/checks/deploys/packages/publishes the next root?
A:

Q020d-091. Should generated SteamPipe VDFs under `output/root/scripts` be considered build/release artifacts, not source files?
A:

Q020d-092. Should default Steam branch activation remain manual, with `--set-live` only for beta branches?
A:

Q020d-093. Should the Steam account name remain operator-provided CLI input and never be hardcoded/configured in repo files?
A:

PIVOT CHECK 5:
If the root package should include toolchains or canonical docs directly, Rustup acquisition and packaging questions need rewrite.

========================================================================
6. SteamCMD, Registry, And Release Authority
========================================================================

Q020d-094. For now, is Steamworks/SteamCMD permission enough to authorize root publish?
A:

Q020d-095. Should SDK root publish consult `Vapor-Registry` permissions before invoking SteamCMD?
A:

Q020d-096. If registry checks are added, should they be advisory diagnostics first or hard blockers immediately?
A:

Q020d-097. Should GitHub identity and Steam identity be bound now, later, or not at all?
A:

Q020d-098. Should SDK manage/acquire SteamCMD app-locally, or may it rely on `steamcmd` from PATH for the near future?
A:

Q020d-099. Should SteamCMD state/login tokens be forced under `$VAPOR_HOME/steam/steamcmd`, or is system SteamCMD state acceptable for now?
A:

Q020d-100. Should SteamCMD-only root publishing remain the first Steam slice before Steamworks API, Workshop UGC, or entitlement checks?
A:

Q020d-101. Should AppID ownership checks gate all SDK usage, only public/published workflows, or only later Workshop workflows?
A:

PIVOT CHECK 6:
If release authority moves to the registry now, root publish implementation and identity questions need rewrite.

========================================================================
7. Rust Toolchain Portability
========================================================================

Q020d-102. Should the Rustup-vs-standalone-archives decision be made only after a relocation test of the current app-root layout?
A:

Q020d-103. What exact relocation test should decide whether current Rustup wrapping is acceptable?
A:

Q020d-104. Is system PATH Rustup acceptable for development bootstrap only?
A:

Q020d-105. Should Steam-distributed SDK installs prefer an app-local `$VAPOR_HOME/rustup/bin/rustup` if Rustup remains the backend?
A:

Q020d-106. Should the next toolchain slice implement app-local Rustup acquisition before considering standalone Rust archives?
A:

Q020d-107. If relocation fails because Rustup metadata dislikes the move, should repair first try to reconcile/reinstall Rustup around the existing toolchain before redownloading everything?
A:

Q020d-108. Should `toolchain install` become idempotent success when the expected toolchain is already present and minimally verified?
A:

Q020d-109. What minimum verification should replace today's `PresentUnverified` file-existence check?
A:

Q020d-110. Should "SDK toolchain install" and "Vapor Toolchain Envelope for native kernel ABI compatibility" be treated as separate concepts?
A:

Q020d-111. What failure evidence would justify returning to standalone official Rust archives?
A:

PIVOT CHECK 7:
If Rustup is rejected or toolchain packaging changes, target and workspace command questions need rewrite.

========================================================================
8. Targets And Cross-Compilation
========================================================================

Q020d-112. Should `x86_64-pc-windows-gnu` become the first Windows cross-build proof target from Arch/Linux?
A:

Q020d-113. Should `x86_64-pc-windows-msvc` remain a later important target because of Steamworks, GUI/native dependencies, or ecosystem expectations?
A:

Q020d-114. Should host triples, installable stdlib targets, linkable build targets, and release-supported targets be modeled separately?
A:

Q020d-115. Should target stdlibs be installed eagerly with the canonical toolchain, or on demand per target command?
A:

Q020d-116. Should linker/tool requirements be explicit SDK status/check output before claiming target support?
A:

Q020d-117. Should Windows GNU proof be allowed even if it cannot be final release policy?
A:

PIVOT CHECK 8:
If Windows target policy changes, toolchain constants and install-plan questions need rewrite.

========================================================================
9. Workspace Execution And Hermeticity
========================================================================

Q020d-118. Should Vapor-managed `cargo check/fmt/build` preserve system PATH after Vapor path prefixes for now?
A:

Q020d-119. Is preserving system PATH incompatible with saying "no system tool leaks"?
A:

Q020d-120. Which environment variables must be scrubbed before claiming hermetic Cargo/Rust execution?
A:

Q020d-121. Should build outputs stay under `$VAPOR_HOME/output/dev/<workspace>` for Vapor-managed commands?
A:

Q020d-122. Should source repo `target/` directories remain unused by Vapor-managed commands?
A:

Q020d-123. Should `workspace sync` prompt because it writes files?
A:

Q020d-124. Should `sdk fmt` prompt because it writes files, or is formatting a normal authoring command?
A:

Q020d-125. Should `workspace deploy` remain an SDK command, or is it really internal root/app maintenance?
A:

Q020d-126. Should SDK replace `xtask` for first-party root work, or should xtask-like internal tooling remain separate?
A:

PIVOT CHECK 9:
If hermeticity expectations change, relocation tests and workspace cleanup need rewrite.

========================================================================
10. Manifest, Artifact, Lockfile, Fingerprint
========================================================================

Q020d-127. Should the current active `Vapor.toml` schema stay minimal for now: workspace identity, content graph, root toolchain pin, and registry metadata?
A:

Q020d-128. Should every generated content crate in `Vapor-Examples/crates/*` have its own `Vapor.toml`?
A:

Q020d-129. Are managed-file headers acceptable TOML comments because they prevent overwriting user-owned files?
A:

Q020d-130. Should SDK fully manage generated Cargo workspace files for custom-content workspaces, or should users own Cargo files and SDK only validate?
A:

Q020d-131. Should seeded `lib.rs` files remain user-owned after creation?
A:

Q020d-132. Should seeded files avoid crate-level intent rustdoc until content semantics are discussed?
A:

Q020d-133. What should be the first `Vapor.lock`-like artifact: packagepack lock, toolchain receipt, root publish receipt, install ledger, canonical glossary migration receipt, or something else?
A:

Q020d-134. Should the first fingerprint implementation be semantic plaintext/TOML first, hash first, or both?
A:

Q020d-135. Which artifact terms may active code use now without causing terminology drift?
A:

Q020d-136. Should active code reserve `root package` for Steam app/depot updates and `Packagepack` for launch composition?
A:

PIVOT CHECK 10:
If the manifest/lockfile first slice changes, pack validation and SDK command questions need rewrite.

========================================================================
11. Packs, Capability, Rhai, Runtime Concepts
========================================================================

Q020d-137. Should the migrated canonical glossary be allowed to answer some pack/capability/Rhai terminology before code implements it?
A:

Q020d-138. Should the next pack implementation validate content-kind child rules before capability/Rhai semantics?
A:

Q020d-139. Should current `engine` content represent a placeholder generic Engine until `core_engine`/`core_mod` artifact layout exists?
A:

Q020d-140. Should current `game` content represent a placeholder generic Game until `base_mod` artifact layout exists?
A:

Q020d-141. Should active code avoid enforcing reserved names like `core_engine`, `core_mod`, and `base_mod` until actual Engine/Game artifact layout exists?
A:

Q020d-142. Should Extension Mods have one attachment parent plus normal dependencies, or can they have multiple attachment parents?
A:

Q020d-143. Should first Rhai work be parser-free file classification before Rhai execution or linting?
A:

Q020d-144. Should SDK `validate` remain manifest/graph-only until Rhai semantics are cleaner?
A:

Q020d-145. What is the smallest capability concept worth implementing in `vapor_core`?
A:

Q020d-146. Should active code avoid names still flagged as suspect: `Vapor Platform`, `Capability Profile`, `Capability Declaration`, `Rhai Asset`, and `Runtime Lock`?
A:

Q020d-147. Should toolchain work avoid all kernel ABI / native plugin compatibility claims until native plugin loading is real?
A:

PIVOT CHECK 11:
If capability/Rhai enters earlier than expected, manifest, lockfile, and toolchain-envelope questions need rewrite.

========================================================================
12. Workshop, Trust, Install Ledger
========================================================================

Q020d-148. Should root app/toolchain/bootstrap and canonical glossary migration stay ahead of Workshop content release work?
A:

Q020d-149. Which repo should own the first Workshop implementation: SDK for upload/publish, Launcher for install/launch, shared core types later?
A:

Q020d-150. Where should authoritative fingerprints live before registry enforcement exists: uploaded content, local lock/receipt, registry, canonical glossary metadata, or all of these?
A:

Q020d-151. Should wording be "trusted but validated for compatibility/integrity" rather than "untrusted hostile content"?
A:

Q020d-152. Should signing fields stay out of active schemas for now?
A:

Q020d-153. Should install ledger belong to Launcher, SDK, shared app-root state, or separate per-workflow receipts?
A:

Q020d-154. Should local dev artifacts ever override Workshop artifacts automatically?
A:

Q020d-155. Should local dev artifact selection require explicit developer/modpack-author action?
A:

Q020d-156. Should Steam Web API/private backend work be avoided until a concrete SteamCMD/Steamworks gap requires it?
A:

PIVOT CHECK 12:
If Workshop moves earlier, root bootstrap, glossary migration, and registry priorities need rewrite.

========================================================================
13. Command Surface And Safety
========================================================================

Q020d-157. Should broad SDK command stubs remain visible for planning, or should commands be hidden until implemented?
A:

Q020d-158. Which SDK command families must become real before further broad command expansion?
A:

Q020d-159. Should current command names be treated as disposable pre-Phase-3 scaffolding?
A:

Q020d-160. Should root SteamPipe publish and future Workshop content publish stay sharply separated in command names?
A:

Q020d-161. Should SDK and Launcher keep shared global flags `--yes`, `--force`, `--strict`, and `--keep-unused-versions` for now?
A:

Q020d-162. Should `--force` semantics be delayed until each mutation is real?
A:

Q020d-163. Should Launcher placeholder output become factual `status: not_implemented` style output?
A:

Q020d-164. Should CLI output eventually support machine-readable JSON for GUI/tooling, or is that premature?
A:

PIVOT CHECK 13:
If command visibility changes, SDK/Launcher cleanup questions need rewrite.

========================================================================
14. Code Cleanup Permission Boundaries
========================================================================

Q020d-165. Is splitting `workspace/manage.rs` into smaller behavior-identical modules approved once an exact file list is presented?
A:

Q020d-166. Is splitting `workspace/deploy.rs` into smaller behavior-identical modules approved once an exact file list is presented?
A:

Q020d-167. Should `toolchain.rs` be split only after relocation testing, unless the extraction is purely mechanical?
A:

Q020d-168. Should SDK get a shared app-root layout module to deduplicate alias/executable/activation paths?
A:

Q020d-169. Is adding or changing module rustdoc allowed only for cold facts unless the exact intent wording has been approved?
A:

Q020d-170. Should SDK/Launcher duplicate CLI safety/spec scaffolding until semantics stabilize, rather than introducing a shared crate?
A:

Q020d-171. Should root package symlink behavior remain "copy target contents" for now?
A:

Q020d-172. Should root package reject directories unless explicit ignore rules exist?
A:

Q020d-173. Should `vapor_core` keep owning the canonical toolchain model, or should that move to SDK later?
A:

Q020d-174. What is the first cleanup slice you would approve after this ledger pass?
A:

PIVOT CHECK 14:
If cleanup permission is narrow, implementation plans must be rewritten as tiny approval packets.

========================================================================
15. Immediate Next Action
========================================================================

Q020d-175. After answering enough of this queue, should the next actual work be a canonical glossary migration approval packet?
A:

Q020d-176. Should that migration packet include exact source paths, destination paths, selected pages, excluded files, `.obsidian` exclusion, status policy, dangling-link list, and no-rewrite guarantees?
A:

Q020d-177. Should the next actual work instead be a relocation-test plan?
A:

Q020d-178. Should the relocation-test plan be presented as exact commands, expected filesystem mutations, and pass/fail criteria before running anything?
A:

Q020d-179. Should the next actual work instead be a dependency-policy patch plan for SDK/Launcher `vapor_core` dependencies?
A:

Q020d-180. Should the next actual work instead be a root-package/self-bootstrap plan?
A:

Q020d-181. Should the next actual work instead be a tiny code-cleanup packet with no policy changes?
A:

Q020d-182. When you stop answering and ask for a pivot check, should I rewrite the unanswered remainder into the next lettered queue before doing any implementation?
A:

END OF 020d
